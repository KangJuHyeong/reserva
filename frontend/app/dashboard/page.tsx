import { PlaceholderPage } from "@/components/placeholder-page";

export default function DashboardPage() {
  return <PlaceholderPage title="Dashboard Coming Next" description="The dashboard route is preserved, but its backend aggregation endpoints are not part of this first frontend slice." />;
}
